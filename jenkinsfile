pipeline {
	agent any
	
		stages{
			stage('first stage'){
				steps{
					bat ' echo "Hi"'
					
				}
			}
			stage('compile stage'){
				steps{
					withMaven(maven : 'MVN_HOME'){
						bat 'mvn clean compile'
					}
				}
			}
		
	}
}