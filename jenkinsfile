pipeline {
	agent any
	stages{
		stage('compile stage'){
			steps{
				withMaven(maven : 'MVN_HOME'){
					sh 'mvn clean compile'
				}
			}
		}
		
		stage('Deployment Stage'){
			steps{
				withMaven(maven : 'MVN_HOME'){
					sh 'mvn deploy'
				}
			}
		}
	}
}