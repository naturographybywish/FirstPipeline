pipeline {
	agent any
		tools {
        	maven 'MVN_HOME' 
   		 }
		stages{
			stage('first stage'){
				steps{
					bat ' echo "Hi"'
					
				}
			}
			stage('compile stage'){
				steps{
						bat 'mvn clean compile'
				}
			}
		
	}
}